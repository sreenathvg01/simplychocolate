import { PopupHostDirectiveDirective } from './popup-host-directive.directive';

describe('PopupHostDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupHostDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
